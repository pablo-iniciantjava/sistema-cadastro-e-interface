# Nome da aplica��o
spring.application.name=projeto-java

# Conex�o com MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/keegoo_devaces_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=Developer
spring.datasource.password=Admin_MySQL@2809
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configura��es do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Porta fixa do servidor Spring Boot (para acessar via navegador/Postman)
server.port=8080

# Configura��o do SSH (utilizada pelos controllers)
# scripts permitidos no servidor de destino (array indexado)
ssh.server.scripts[0]=/home/m1113r/teste.sh
ssh.server.scripts[1]=/home/m1113r/atualizar.sh



